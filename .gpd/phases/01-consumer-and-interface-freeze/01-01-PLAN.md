# Phase 01 Plan 01: Consumer And Interface Freeze

## Objective

Decide whether a real consumer forces a glyph-engine package boundary and, if
so, define the smallest interface and ablation before extraction.

## Tasks

1. Read `SOVEREIGN_PRD.md`, `SOURCE_BOUNDARY.md`, `DATA_POLICY.md`, and
   `TODO.md`.
2. Pick the first admitted consumer or reject extraction for now.
3. Define the minimal interface and candidate source files.
4. Define the tiny fixture and borrowed baseline ablation.
5. Update `SOURCE_BOUNDARY.md`, `TODO.md`, `.gpd/STATE.md`, and
   `.gpd/state.json`.

## Pass Condition

Extraction becomes a bounded code task with source paths, destination paths,
fixture, consumer, and ablation all named. If that cannot be done, the lane
stays a hold surface.
